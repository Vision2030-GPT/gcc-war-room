# Anthropic API Key — required for AI Analyst (Live mode) and Live Intel features
# Get your key at https://console.anthropic.com/
# The dashboard works without this (offline mode), but live AI features need it
VITE_ANTHROPIC_API_KEY=sk-ant-xxxxx
